1.int[] --int[]内对应存储的是一个大数的每一位，计算这个大数的时候直接 
 for(int i=0;i<Lenght;i++) sum = sum*10 + int[i]
 而不用从后往前用mi来计算这个大数；
 ------------------------------------以前的那个方法很耗时、很蠢
 2.string.trim()
   一个裁剪函数，返回string去掉首部和尾部的空格以后的字符串。
   这样就可以不用自己写函数判断一个string是从什么地方开始字符的了。
   
  3.人傻就要多读书
  
  
